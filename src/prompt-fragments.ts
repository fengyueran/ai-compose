export type PromptFragment = {
  id: string
  title: string
  source: 'preset' | 'user'
  items: string[]
}

export const presetPromptFragments: PromptFragment[] = [
  {
    id: 'core-principles',
    title: '核心原则',
    source: 'preset',
    items: [
      '优先采用 Agent-First 思路：复杂任务优先交给专门能力、专门角色或明确分工处理',
      '默认采用 Test-Driven 思路：先写测试，再实现功能，验证通过后再整理代码',
      '默认采用 Security-First 思路：绝不为了开发速度牺牲安全性，所有输入都应校验',
      '默认避免直接修改原对象，优先返回新的数据结果，减少隐式副作用',
      '复杂功能、重构或架构调整前，先计划再执行，不直接跳进实现细节',
    ],
  },
  {
    id: 'security-guidelines',
    title: '安全指南',
    source: 'preset',
    items: [
      '任何提交前都不得留下硬编码密钥、密码、令牌或其他敏感信息',
      '所有用户输入和系统边界输入都必须显式校验',
      '涉及数据库访问时，默认使用参数化查询，避免注入风险',
      '涉及 HTML 或外部内容渲染时，默认考虑 XSS 风险并做净化或转义',
      '涉及权限边界时，默认验证认证与授权逻辑是否正确',
      '错误信息应可诊断，但不得泄露敏感数据或内部细节',
      '严禁在代码、配置或文档中硬编码密码、密钥、令牌、证书、私有地址或其他敏感信息',
      '敏感配置必须来自环境变量或安全配置源',
      '发现高危安全问题时，应先停止扩散、修复问题并复查代码库中的同类风险',
    ],
  },
  {
    id: 'coding-style',
    title: '代码风格',
    source: 'preset',
    items: [
      '默认避免直接修改原对象，优先返回新的对象或结果，减少副作用',
      '所有新增文件夹和文件名默认使用 Kebab Case（全小写，单词间用短横线连接）',
      '函数保持短小，单个函数尽量不超过 50 行',
      '单个文件应保持聚焦，通常不超过 800 行',
      '控制代码嵌套层级，尽量不要超过 4 层',
      '函数职责单一，模块边界清晰，保持高内聚、低耦合',
      '优先使用解决当前问题的最小实现；不要增加未被要求的功能、抽象、配置或猜测性扩展',
      '错误处理要覆盖到每一层，不要静默吞掉错误',
      '界面层错误应给用户清晰反馈，系统侧日志应保留足够上下文',
      '默认重视可读命名、错误处理和输入校验，不要把这些留到后补',
    ],
  },
  {
    id: 'testing-requirements',
    title: '测试要求',
    source: 'preset',
    items: [
      '最低测试覆盖率目标为 80%',
      '默认覆盖单元测试、集成测试和关键用户流程测试，而不是只做局部验证',
      '新功能或缺陷修复默认遵循 TDD：先写失败测试，再写最小实现，最后整理代码',
      '默认遵循 RED → GREEN → REFACTOR：先让测试失败，再让测试通过，再在不破坏测试的前提下整理代码',
      '将任务改写为可验证的成功标准；优先编写能复现问题或约束行为的测试，再实现并验证通过',
      '修改完成后，必须通过测试、构建、检查或可观察结果确认没有明显回退',
      '测试失败时，先检查测试隔离、mock 和实现问题，不要默认修改测试去迎合当前错误实现',
    ],
  },
  {
    id: 'development-workflow',
    title: '开发流程',
    source: 'preset',
    items: [
      '默认遵循 Plan → TDD → Review → Commit 的工作流，而不是跳过中间环节直接提交',
      '实现前先规划依赖、风险与阶段目标，再开始写代码',
      '编码前先显式说明假设、歧义与权衡；信息不足时先澄清，不要静默猜测后直接实现',
      '默认先写测试，再实现，再重构',
      '代码完成后应立即补一次代码审查，并优先处理 CRITICAL 和 HIGH 问题',
      '涉及安全敏感路径时，应优先补一次安全审查',
      '复杂任务优先先拆解分工或阶段，再决定是否并行推进',
      '个人临时信息、调试笔记和短期上下文应放在合适的 memory，而不是散落到正式文档中',
      '团队知识、架构决策、接口变化和长期可复用信息应沉淀到项目现有文档结构中',
      '当前任务已经通过代码、注释或现有文档表达清楚的信息，不要重复写到新的文档位置',
    ],
  },
  {
    id: 'git-workflow',
    title: 'Git 工作流',
    source: 'preset',
    items: [
      '提交信息默认使用 Conventional Commits 格式，例如 feat、fix、refactor、docs、test、chore、perf、ci',
      '准备 PR 时，应基于完整提交历史整理清晰总结和验证计划',
      '推送分支时默认使用 -u，保证后续跟踪关系清晰',
      '工作区可能存在用户已有改动或生成内容，除非明确要求，不要回退、覆盖或清理不属于当前任务的修改',
      '避免使用会无条件丢弃改动的破坏性命令，例如强制重置、强制覆盖或批量清理',
      '合并功能分支或修复分支到主分支时，优先使用 --no-ff，保留完整分支历史，便于回溯和管理',
    ],
  },
  {
    id: 'code-review',
    title: '代码审查',
    source: 'preset',
    items: [
      '审查时优先识别行为错误、回归风险、边界条件缺失、错误处理不足和测试空缺',
      '不先讨论风格偏好，先处理真正影响结果的问题',
      '优先给出高风险、高影响、可复现的问题，并说明影响和修复建议',
      '只修改完成当前任务所必需的代码；不要顺手“改进”相邻代码、注释、格式或未损坏的实现',
      '判断问题时尽量基于代码、配置、行为或验证结果，而不是停留在主观偏好',
      '如果仍有不确定性，应明确标注为假设或待确认项',
    ],
  },
  {
    id: 'knowledge-and-collaboration',
    title: '知识沉淀与协作',
    source: 'preset',
    items: [
      '除代码块内的注释和字符串外，所有回复内容默认使用中文',
      '输出结果时，先给关键结论，再给必要说明、验证结果和后续建议',
      '避免冗长铺垫和重复表述',
      '沟通应简洁、直接、友好，以帮助推进任务为目标',
      '如果执行中基于某些假设推进，或有环境、权限、验证限制，应明确写出前提与限制',
      '把用户当作协作对象而不是审查对象',
    ],
  },
]
